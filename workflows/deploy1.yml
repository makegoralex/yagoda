name: Deploy to REG.RU

on:
  push:
    branches:
      - main 
jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 37.140.192.191:22             
          username: u1618701            
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftps                  
          port: 22                        
          local-dir: ./                   
          server-dir: /www/yagoda-coffee58.ru/ 
          exclude: |
            .github*
            **/.git*
            **/node_modules/**






